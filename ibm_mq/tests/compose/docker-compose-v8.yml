version: '3'

# Adapted from https://github.com/dwilbraham/docker-compose-etcd
services:
  ibmmq:
    build:
      context: ${COMPOSE_DIR}/v8
    environment:
      - MQ_QMGR_NAME=datadog
      - LICENSE=accept
    ports:
      - "1414:1414"
